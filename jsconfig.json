{
  "compilerOptions": {
    "baseUrl": ".", // Asegúrate de incluir "baseUrl"
    "paths": {
      "@/*": ["./src/*"] // Definición del alias
    },
    // Otras configuraciones que puedas tener
  }
}
